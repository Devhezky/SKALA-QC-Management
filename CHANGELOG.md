# Changelog

## [Unreleased]

### Changed
- Updated API connection URLs to point to production (`https://skala.narapatistudio.com`) instead of localhost.
- Updated default API Settings examples to reflect production URLs.
- Removed `/index.php` from frontend and backend API calls to support clean URLs and prevent 404 errors.
